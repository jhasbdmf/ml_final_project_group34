you can either run one jupyter notebook final_project_group34.ipynb. It contains both MLP and CNN.
Alternatively, you can run numpy_mlp.py and numpy_cnn.py separately. 

In both cases, you need to have the following files in the same directory as the aforementioned notebook
and/or *.py files.